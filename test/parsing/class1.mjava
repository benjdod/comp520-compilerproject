class hello {

    public static int terrible_int;

    private void h(int a, boolean b, int c) {
        terrible_int = terrible_int + 2;
        int x = a * c / 6;
        boolean t = b;

        if (x = 5) {
            int y = 15;
        }

        return;
    }
}